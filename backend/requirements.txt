# Kern-Backend (Phase 1)
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6

# Später (Phasen 2+): Migrationen und Excel-Export
alembic>=1.13
openpyxl>=3.1

# Hardware-Transport.
# USB-Drucker: benötigt zusätzlich die System-Bibliothek libusb
#   sudo apt install -y libusb-1.0-0
# Netzwerkdruck (Ethernet) benötigt keine Zusatzbibliothek.
pyusb>=1.2

# Test
pytest>=8.0
httpx>=0.27
